name: Hybrid Retrieval CI

on:
  push:
    paths:
      - "labs/hybrid-retrieval/**"
      - ".github/workflows/lab-hybrid-retrieval-ci.yml"
  pull_request:
    paths:
      - "labs/hybrid-retrieval/**"
      - ".github/workflows/lab-hybrid-retrieval-ci.yml"

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: labs/hybrid-retrieval
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: labs/hybrid-retrieval/pyproject.toml
      - name: Install dependencies
        run: pip install -e '.[dev]'
      - name: Ruff
        run: ruff check .
      - name: MyPy
        run: mypy src
      - name: Tests
        run: pytest --maxfail=1 -q
