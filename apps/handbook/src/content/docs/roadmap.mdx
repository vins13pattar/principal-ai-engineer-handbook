---
title: Roadmap
description: Current platform version and the content authoring schedule ahead.
version: 2.14.0
lastUpdated: 2026-08-28
revisionHistory:
  - version: 1.0.0
    date: 2026-08-04
    change: Replaced the old static-site roadmap with the platform-first roadmap.
    author: vinod
  - version: 1.1.0
    date: 2026-08-05
    change: Marked v0.2 shipped (Module 0 and Module 1 published).
    author: vinod
  - version: 1.2.0
    date: 2026-08-05
    change: Marked v0.3 shipped (Module 2, Module 3, and the first Architecture page published).
    author: vinod
  - version: 1.3.0
    date: 2026-08-05
    change: Marked v0.4 shipped for Modules 4-7. Split the labs schedule out of the version table since a lab is a much larger unit of work than a module and shouldn't gate a version bump.
    author: vinod
  - version: 1.4.0
    date: 2026-08-07
    change: "Marked v0.5 shipped (Module 8: RAG, Module 9: Model Serving)."
    author: vinod
  - version: 1.5.0
    date: 2026-08-07
    change: "Marked v0.6 shipped (Module 10: Kubernetes, Module 11: Cloud)."
    author: vinod
  - version: 1.6.0
    date: 2026-08-07
    change: "Marked v0.7 shipped (Module 12: Observability)."
    author: vinod
  - version: 1.7.0
    date: 2026-08-09
    change: Recorded the AI Systems Coding track and narrowed the remaining v0.8 scope to Modules 13-14 and the leadership track.
    author: vinod
  - version: 1.8.0
    date: 2026-08-09
    change: Recorded the first three Reference lookups and the enforced lookup page contract.
    author: vinod
  - version: 1.9.0
    date: 2026-08-09
    change: Marked Module 13 (System Design) published and narrowed v0.8 to Module 14.
    author: vinod
  - version: 2.0.0
    date: 2026-08-10
    change: Marked v0.8 shipped — Module 14 completes the Learn track. Closed out the lab migration section, which is finished.
    author: vinod
  - version: 2.1.0
    date: 2026-08-10
    change: Recorded the Technical Leadership track; v0.9 now scopes to Reference lookups and Cheat Sheets.
    author: vinod
  - version: 2.2.0
    date: 2026-08-10
    change: Recorded six more Reference lookups; only the four fast-moving topics remain.
    author: vinod
  - version: 2.3.0
    date: 2026-08-10
    change: Opened Cheat Sheets, scoped to activities rather than technologies, with its own enforced contract.
    author: vinod
  - version: 2.4.0
    date: 2026-08-10
    change: Cheat Sheets complete; v0.9 now scopes to the four fast-moving Reference lookups alone.
    author: vinod
  - version: 2.5.0
    date: 2026-08-12
    change: >-
      Marked v0.9 shipped — the four fast-moving Reference lookups (MCP, LangGraph, RAG, Vector DB)
      are published, each declaring what it was verified against. Reference is complete.
    author: vinod
  - version: 2.6.0
    date: 2026-08-14
    change: >-
      Scoped v0.10 — Module 15 (Agent Identity and Access), covering agent-scoped short-lived
      credentials versus blanket on-behalf-of delegation, and authorization for remote MCP servers.
      Added its companion lab to the Build schedule. Raised from a reader suggestion.
    author: vinod
  - version: 2.6.1
    date: 2026-08-14
    change: Credited Shantanu Lodh, who raised the Module 15 suggestion, by name.
    author: vinod
  - version: 2.7.0
    date: 2026-08-14
    change: >-
      Marked v0.10 shipped — Module 15 (Agent Identity and Access) is published, written against the
      MCP 2026-07-28 authorization spec and the OAuth RFCs beneath it. The Learn track is sixteen
      modules. Its companion lab remains planned.
    author: vinod
  - version: 2.8.0
    date: 2026-08-14
    change: >-
      Built the Agent Identity Broker lab, so Module 15's blast-radius claim is measured rather than
      asserted. Eight labs.
    author: vinod
  - version: 2.9.0
    date: 2026-08-14
    change: >-
      Built the Model Router lab. Its escalation measurement contradicted the assumption the tests
      were first written around; the result is recorded rather than smoothed over. Nine labs.
    author: vinod
  - version: 2.10.0
    date: 2026-08-14
    change: >-
      Built the Semantic Cache lab. Its threshold sweep found no setting that serves a genuine
      paraphrase before a wrong one, with an explicit account of how much of that is the simulated
      embedding. Ten labs.
    author: vinod
  - version: 2.11.0
    date: 2026-08-14
    change: >-
      Built the Evaluation Platform lab, completing the planned lab schedule. Eleven labs, none
      planned. Its sample-size tables put a number on how much most evaluation claims cannot see.
    author: vinod
  - version: 2.12.0
    date: 2026-08-15
    change: >-
      Corrected three counts that successive bulk updates had left false — architecture coverage,
      the size of the migrated lab cohort, and the SDK's internal tools/list, which does carry the
      protocol _meta stamp.
    author: vinod
  - version: 2.13.0
    date: 2026-08-28
    change: >-
      Recorded v0.11 — the audio series. Every one of the 63 content pages has an episode, which the
      roadmap had gone thirteen days without mentioning while claiming to say what does not exist
      yet.
    author: vinod
  - version: 2.14.0
    date: 2026-08-28
    change: >-
      Marked v0.12 shipped — the four missing design reviews. Every lab now has an architecture page,
      which closes two of the three v1.0 criteria and leaves the third resting on a decision rather
      than on writing. Recorded the four new pages as having no episode yet.
    author: vinod
category: section-overview
---

## Current version: v0.11.0 — The audio series

The documentation platform (v0.1.0) and all sixteen Learn modules are complete:
[Module 0](/learn/modules/00-principal-engineer-mindset/) · [Module 1](/learn/modules/01-production-python/) ·
[Module 2](/learn/modules/02-distributed-systems/) · [Module 3](/learn/modules/03-networking/) ·
[Module 4: AI Infrastructure](/learn/modules/04-ai-infrastructure/) ·
[Module 5: Agent Engineering](/learn/modules/05-agent-engineering/) ·
[Module 6: MCP](/learn/modules/06-mcp/) · [Module 7: LangGraph](/learn/modules/07-langgraph/) ·
[Module 8: RAG](/learn/modules/08-rag/) · [Module 9: Model Serving](/learn/modules/09-model-serving/) ·
[Module 10: Kubernetes](/learn/modules/10-kubernetes/) · [Module 11: Cloud](/learn/modules/11-cloud/) ·
[Module 12: Observability](/learn/modules/12-observability/) ·
[Module 13: System Design](/learn/modules/13-system-design/) ·
[Module 14: Leadership](/learn/modules/14-leadership/) ·
[Module 15: Agent Identity](/learn/modules/15-agent-identity/).
The [Architecture](/architecture/) section has a design-review page for **all eleven labs**, each
with a running implementation behind it. [Build](/build/) documents every lab from a
run-it-and-verify-it angle. The [Interview](/interview/) section now covers all three rounds:
[AI Infrastructure System Design](/interview/tracks/ai-infrastructure-system-design/),
[AI Systems Coding](/interview/tracks/ai-systems-coding/), and
[Technical Leadership](/interview/tracks/technical-leadership/). [Reference](/reference/) is
complete: thirteen lookups against a five-section page contract that CI enforces, with the four
fast-moving ones — [MCP](/reference/lookups/mcp/), [LangGraph](/reference/lookups/langgraph/),
[RAG](/reference/lookups/rag/), and [Vector DB](/reference/lookups/vector-db/) — each declaring the
release it was verified against, on a 90-day review window CI fails once a page exceeds.
[Cheat Sheets](/cheatsheets/) is complete: five activity-scoped sheets against a four-section
contract CI enforces — three for interview rounds, two for the work itself.

Almost every one of those pages can also be listened to. The [audio
series](#the-audio-series-shipped) covers 63 of the 67 content pages — ten hours in total, with the
four newest architecture pages awaiting a recording pass.

## What's next

| Version    | Scope                                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------------------------ |
| ~~`v0.2`~~ | ~~Learn Module 0 (Principal Engineer Mindset) and Module 1 (Production Python)~~ — shipped 2026-08-05        |
| ~~`v0.3`~~ | ~~Learn Modules 2–3 (Distributed Systems, Networking) and the first Architecture page~~ — shipped 2026-08-05 |
| ~~`v0.4`~~ | ~~Learn Modules 4–7 (AI Infrastructure, Agent Engineering, MCP, LangGraph)~~ — shipped 2026-08-05            |
| ~~`v0.5`~~ | ~~Learn Modules 8–9 (RAG, Model Serving)~~ — shipped 2026-08-07                                              |
| ~~`v0.6`~~ | ~~Learn Modules 10–11 (Kubernetes, Cloud)~~ — shipped 2026-08-07                                             |
| ~~`v0.7`~~ | ~~Learn Module 12 (Observability)~~ — shipped 2026-08-07                                                     |
| ~~`v0.8`~~ | ~~Learn Module 14 (Leadership)~~ — shipped 2026-08-10                                                        |
| ~~`v0.9`~~ | ~~The four fast-moving Reference lookups (MCP, LangGraph, RAG, Vector DB), each with a `freshness` block~~ — shipped 2026-08-12 |
| ~~`v0.10`~~ | ~~Learn Module 15 (Agent Identity and Access) — agent-scoped credentials and remote MCP authorization~~ — shipped 2026-08-14 |
| ~~`v0.11`~~ | ~~The audio series — a generated episode on every content page, with its transcript~~ — shipped 2026-08-23 |
| ~~`v0.12`~~ | ~~The four missing design reviews — agent identity, model routing, semantic caching, model evaluation~~ — shipped 2026-08-28 |
| `v1.0`     | Complete, cross-linked handbook: every Learn module has an Architecture page, a lab, and interview coverage  |

### What v1.0 still needs

One criterion remains, and it cannot be met as written:

- **Architecture coverage is done.** All eleven labs have a design review, as of v0.12:
  [Agent Identity Platform](/architecture/systems/agent-identity-platform/),
  [Task-Based Model Routing](/architecture/systems/task-based-model-routing/),
  [Semantic Response Caching](/architecture/systems/semantic-response-caching/), and
  [Continuous Model Evaluation](/architecture/systems/continuous-model-evaluation/) closed the gap.
- **Interview coverage is done.** All sixteen modules carry embedded questions, and all three rounds
  have a track.
- **Five modules without a lab**, which makes "every module has a lab" unreachable as stated.
  Modules [7](/learn/modules/07-langgraph/) and [11](/learn/modules/11-cloud/) are lab-shaped and
  could get one. Modules [0](/learn/modules/00-principal-engineer-mindset/),
  [13](/learn/modules/13-system-design/), and [14](/learn/modules/14-leadership/) are not — a lab
  about the Principal Engineer mindset would be a lab in name only. The criterion will be restated
  to cover the modules a lab can honestly serve, rather than left standing as a target that can only
  be hit by building three labs nobody should run.

That leaves v1.0 waiting on one decision and at most two labs, rather than on a body of writing.

Labs are tracked separately from module versions, since a production-quality lab is a much larger
unit of work than a module and shouldn't gate a version bump the way a module does. As of
2026-08-14 the planned schedule is complete — every lab below is built:

| Lab                                                               | Companion module | Status            |
| ----------------------------------------------------------------- | ---------------- | ----------------- |
| [Async AI Gateway](/build/labs/async-ai-gateway/)                 | Module 1, 3      | Production-ready  |
| [SLO-Driven AI Operations](/build/labs/slo-driven-ai-operations/) | Module 10, 12    | Production-shaped |
| [Durable Agent Task Engine](/build/labs/durable-agent-task-engine/) | Module 2, 5    | Production-shaped |
| [Policy-Gated Tool Runtime](/build/labs/policy-gated-tool-runtime/) | Module 5     | Production-shaped |
| [Hybrid Retrieval and Evaluation](/build/labs/hybrid-retrieval/) | Module 8 | Production-shaped |
| [Dynamic Batching Inference](/build/labs/dynamic-batching-inference/) | Module 9 | Production-shaped |
| [Multi-Tenant MCP Server](/build/labs/multi-tenant-mcp-server/) | Module 6 | Production-shaped |
| [Agent Identity Broker](/build/labs/agent-identity-broker/)       | Module 15, 6     | Production-shaped |
| [Model Router](/build/labs/model-router/)                         | Module 4         | Production-shaped |
| [Semantic Cache](/build/labs/semantic-cache/)                     | Module 4         | Production-shaped |
| [Evaluation Platform](/build/labs/evaluation-platform/)           | Module 4, 12     | Production-shaped |

### The audio series: shipped

Sixty-three episodes, **ten hours** of audio,
from 3:00 on the shortest ADR to 22:24 on [Module 6](/learn/modules/06-mcp/) — measured from the
episode manifests, not estimated. The model plans the arc and writes the two-voice dialogue; a local
text-to-speech model speaks it.

The ten hours are worth stating plainly because
[`docs/PODCAST.md`](https://github.com/vins13pattar/principal-ai-engineer-handbook/blob/main/docs/PODCAST.md)
projected seven, from the handbook's 414 minutes of prose. Spoken dialogue expands on the page it
covers rather than reading it aloud, and the estimate did not account for that. The number here is
the measured one.

The 63 were every page that said something when the series ran. The ten pages that do not — [Start
Here](/start-here/), this roadmap, and the eight section indexes — are navigation, and an episode
narrating a table of contents would be filler. That is a deliberate exclusion, not a gap to close.

The four architecture pages published in v0.12 have no episode yet, which is the ordinary state of a
new page: writing and recording are separate passes, and the second costs money. **Four pending**,
and this line is where that count is kept honest.

Two things about the pipeline are recorded rather than assumed. Speech runs locally, so the only
money the series spends is on the language model; and every transcript is committed next to its
manifest, so re-rendering a page's episode text never pays the model twice.
[ADR-0008](/adr/decisions/0008-typescript-podcast-pipeline/) covers why the engine is built against
two ports — `LlmPort` and `TtsPort` — with no vendor SDK imported anywhere behind them, which is
what lets the whole pipeline be tested without a network or an API key.

Audio is served from a separate origin and is not in the repository; the transcripts are, so the
words survive independently of the hosting.

### Module 15 — Agent Identity and Access: shipped

Suggested by [Shantanu Lodh](https://www.linkedin.com/in/shanlodh/), and the gap is real: the
handbook covers what an agent is allowed to *do* without covering who it *is*. [Module 5](/learn/modules/05-agent-engineering/) and the
[Policy-Gated Tool Runtime](/build/labs/policy-gated-tool-runtime/) enforce capability scopes and
approvals, and [Module 6](/learn/modules/06-mcp/#security) argues that authorization belongs on the
transport rather than in per-request metadata — but none of them answer what identity the transport
is carrying.

The module is scoped around two questions:

- **Agent-scoped credentials versus blanket delegation.** Treating every agent action as
  on-behalf-of the user is the default because it is the easiest thing to build, and it makes the
  agent's blast radius equal to the user's entire permission set. Short-lived credentials minted per
  agent — narrowed in audience, scope, and lifetime — bound the damage, make actions attributable to
  the agent rather than the human, and make revocation something other than all-or-nothing.
- **Authorization for remote MCP servers.** A remote server has to decide whether a caller is
  allowed to invoke a tool, and the answer cannot be "the client said so". That means the host
  registering with an identity provider, users authenticating through OIDC/OAuth, and the server
  validating audience and scope **server-side** on every call — the same argument
  [Module 6](/learn/modules/06-mcp/#security) makes about the transport, taken one layer down into
  what the token actually asserts.

[Module 15](/learn/modules/15-agent-identity/) is published, written against the MCP `2026-07-28`
authorization specification and the OAuth RFCs beneath it — token exchange (RFC 8693), resource
indicators (RFC 8707), and the audience validation the specification requires of every MCP server.

The companion lab, [**Agent Identity Broker**](/build/labs/agent-identity-broker/), is built. It
mints scoped short-lived tokens, validates audience and scope server-side, and measures the
blast-radius difference across a three-server fleet — one tool opened by a fully narrowed token,
two when only the audience is narrowed. CI runs a mutation job that disables audience verification
and fails the build if the suite stays green.

### Lab migration: complete

A parallel branch built six labs against the retired static-HTML prototype before this platform
existed. All six now live in `labs/`, each re-verified and honestly re-labelled on arrival rather
than moved wholesale. That branch has been deleted; nothing on it is unique. The other five labs —
the [Async AI Gateway](/build/labs/async-ai-gateway/) and the four built in August 2026 for agent
identity, model routing, semantic caching, and evaluation — were written on this platform and never
migrated.

Five were straightforward ports:
[SLO-Driven AI Operations](/build/labs/slo-driven-ai-operations/),
[Durable Agent Task Engine](/build/labs/durable-agent-task-engine/),
[Policy-Gated Tool Runtime](/build/labs/policy-gated-tool-runtime/),
[Hybrid Retrieval and Evaluation](/build/labs/hybrid-retrieval/), and
[Dynamic Batching Inference](/build/labs/dynamic-batching-inference/).

The [Multi-Tenant MCP Server](/build/labs/multi-tenant-mcp-server/) was **rebuilt rather than
migrated**. The donor version targeted `2025-11-25` and described itself as "MCP-style", so porting
it would have imported a protocol that no longer exists. It is now a real MCP server on the official
SDK (`mcp` 2.0.0, which reports `2026-07-28` as its only modern protocol version), tested through
the SDK's own client over Streamable HTTP.

That rebuild took two attempts, and the failed one is recorded because it is the more useful half:
**a credential carried in per-request `_meta` cannot secure an MCP server.** The SDK issues protocol
calls of its own — `call_tool()` internally calls `validate_tool_result()`, which issues its own
`tools/list`. Both requests carry the SDK's protocol `_meta` stamp, but only the first can carry the
application's: `call_tool()` takes a `meta=` argument and `list_tools()` has none. So a server
authorizing on `_meta` rejects its own client, and a server that exempts `tools/list` to compensate
has reopened the hole. Authorization sits on the
transport, where every request carries it. See
[Module 6](/learn/modules/06-mcp/#security) and the lab's own write-up.

The [Async AI Gateway](/build/labs/async-ai-gateway/) needed no migration but did need a fix: it
was labelled `production-ready` while its CI omitted a dependency extra, so test collection aborted
before anything ran — masking 13 type errors and a failing test. A label is a claim, and that one
was not being checked.

## How this roadmap works

Each version bump ships complete modules, not partial ones — a module isn't "in progress" on this
roadmap, it's either published (with every required section from the [Learn](/learn/) structure)
or not started. `scripts/lint-content-structure.ts` enforces that a merged module page has every
required section, so the roadmap can't silently drift from what's actually on the site. Labs follow
their own schedule in the table above rather than gating a module's version bump — a module ships
once its content is complete, whether or not its companion lab exists yet.

Fast-moving pages carry a standing obligation the roadmap does not otherwise track. Each declares
`verifiedAgainst` and `verifiedOn`, and CI fails the build once the page passes its 90-day review
window — so the next re-verification pass is scheduled by the linter rather than by anyone
remembering. The pages published in v0.9 come due in **November 2026**. Two of them —
[RAG](/reference/lookups/rag/) and [Vector DB](/reference/lookups/vector-db/) — cover subject matter
with no version of its own, so each names a real implementation baseline (LangChain and Pinecone's
dated API) instead. A fabricated version number would satisfy the linter and mean nothing.

Track progress against this roadmap through the repository's
[releases](https://github.com/vins13pattar/principal-ai-engineer-handbook/releases) and
[commit history](https://github.com/vins13pattar/principal-ai-engineer-handbook/commits/main).
