---
import Callout from "./Callout.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Callout variant="success" glyph="✓" label="Interview Answer" title={title}>
  <slot />
</Callout>
