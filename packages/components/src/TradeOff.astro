---
import Callout from "./Callout.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Callout variant="warning" glyph="⇄" label="Trade-off" title={title}>
  <slot />
</Callout>
