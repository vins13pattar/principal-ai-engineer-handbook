name: SLO-Driven AI Operations CI

on:
  push:
    paths:
      - "labs/slo-driven-ai-operations/**"
      - ".github/workflows/lab-slo-driven-ai-operations-ci.yml"
  pull_request:
    paths:
      - "labs/slo-driven-ai-operations/**"
      - ".github/workflows/lab-slo-driven-ai-operations-ci.yml"

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: labs/slo-driven-ai-operations
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: labs/slo-driven-ai-operations/pyproject.toml
      - name: Install dependencies
        run: pip install -e '.[dev]'
      - name: Ruff
        run: ruff check .
      - name: MyPy
        run: mypy src
      - name: Tests
        run: pytest --maxfail=1 -q
