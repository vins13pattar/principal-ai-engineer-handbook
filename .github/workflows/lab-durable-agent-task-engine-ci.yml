name: Durable Agent Task Engine CI

on:
  push:
    paths:
      - "labs/durable-agent-task-engine/**"
      - ".github/workflows/lab-durable-agent-task-engine-ci.yml"
  pull_request:
    paths:
      - "labs/durable-agent-task-engine/**"
      - ".github/workflows/lab-durable-agent-task-engine-ci.yml"

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: labs/durable-agent-task-engine
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: labs/durable-agent-task-engine/pyproject.toml
      - name: Install dependencies
        run: pip install -e '.[dev]'
      - name: Ruff
        run: ruff check .
      - name: MyPy
        run: mypy src
      - name: Tests
        run: pytest --maxfail=1 -q
